what is the idea
the idea is to learn modeling and dynamics then apply controllers to it,
one of the most generic problems is the inverted pendulum, so it has alot of people whom already has done it before, so in the learning process, i do not really need to invent a new solution.
so basically after making the models in mujoco i will start getting its dynamics and state-space model 
after that iam gonna add them in matlab so it will be our ui, playing with variables such as the damping coeff. or friction or event the model it self (it is kind of easy task), why -> in designing a rl model i do not want to be a generic run it for 1000hrs and it will learn eventually, i wanted to make it more of a global model for multilink inverted pendulum.

so what are the algorithms iam looking to implement
for conventional: 
  pid and lqr

for hybrid ones:
  fuzzy pid

for intellegent:
  mainly ppo, anfis, (anfis is basically a fuzzy controller with a rl model trying the best design it is simple yet effective)
  if i got time i will go with fppo

so the plan is to publish a small part in the jac-ecc (the simple thing) and for the more advanced model, i will try to reach abroad for more prestigious lab.
the timeline:
  finish the modelling (state-space & dynamics) by 30 or by max 31st of august
  finish the lqr and pid systems for it in matlab by 3rd of sep by max
  start working on dqn for the single pendulum and double if viable.
  try to move from single to triple using ppo
  try to add imitation learning with goal of making a general controller for physically undefined double pendulum (no mass or length are defined), but use imitaiton with an optimal controller (iam picking mcp as a part of my learning journey) to give it an example between each model and the other:
    for this i would actually prefer doing the single pendulum first since there is a dozen types of optimal controllers that need a look before moving to the imitation

  last but not least the tip training (i have stumpled on that video for the tip swingup, it was brilliant, i would like to use it as a pretrain then move with ppo/sac who knows, maybe we might generalize it too.

